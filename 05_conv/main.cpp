#include <stdint.h>

// template <typename T>
// int cpu_conv(T **act, T **wei, T **result) {
//   for (size_t o_in = 0; o_in < O; ++o_in) {
//   }
//   return 0;
// }
/**
 * @brief simple convolution kernel
 *  kernel size is 2x2; activation is 8x8x4x4
 */
int main() {return 0;}
